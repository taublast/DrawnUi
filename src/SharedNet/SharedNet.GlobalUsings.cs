// SharedNet target-shim layer for non-MAUI consumers.
// Intentionally NO `global using Microsoft.Maui;` — shims live in
// DrawnUi.Draw namespace, so they coexist with real MAUI types when
// each target compiles its own assembly.
